
Есть два символа которые реализуют простое ветвление:

1) && - выполнит правую команду, если левая пройдёт успешно
2) || - выполнит правую команду, только если левая выдаст ошибку

Примеры:
``` bash
cat file && echo "File was found"
cat file || echo "File wasn't found"
```