
Это файл, который есть в каждой домашней директории. Он запускается как скрипт при запуске [[Bash|bash]] (т.е. при входе в учётную запись). 

Этот файл используется, например, для настройки [[Переменные окружения|переменных окружения]], псевдонимов и т.д.