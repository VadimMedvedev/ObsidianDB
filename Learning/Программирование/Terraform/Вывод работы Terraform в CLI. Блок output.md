
Для вывода каких-то данных при развёртывании инфраструктуры, мы можем использовать блоки output. Они выводят некоторые данные в терминал из которого запускается terraform.

### Определение блоков `output`

В каждом блоке `output` мы указываем аргументы `description` и `value` в которых указываем описание возвращаемых данных и значение соответсвенно. Также в лейбле мы должны указать название блока `output`. Пример:
``` HCL
output "instance_id" {
  description = "ID of the EC2 instance"
  value       = aws_instance.web.id
}
```

### Поулчение данных

В зависимости от того, как мы запускаем модуль в terraform, эти выводы будут выодиться по разном.
Если мы запускаем модуль как корневой, то результаты вывода будут писаться в консоль. Но если какой-то дочерний модуль будет возвращать данные, то мы можем получить их только из родительского модуля.
Родительский модуль может получить данные от дочернего модуля следующим образом `module.<CHILD_MODULE_NAME>.<OUTPUT_NAME>`.