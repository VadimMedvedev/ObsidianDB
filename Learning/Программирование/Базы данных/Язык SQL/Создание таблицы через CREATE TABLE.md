
``` SQL
CREATE TABLE название таблицы (
	название_поля тип_данных,
	название_поля2 тип данных2(длина, при необходимости),
	PRIMARY KEY название тип,
	
)
```

`PRIMARY KEY` указывает на то, что это первичный ключ.
