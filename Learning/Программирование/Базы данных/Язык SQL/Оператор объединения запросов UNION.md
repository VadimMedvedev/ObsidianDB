
``` SQL
Запрос1 (SELECT)
UNION
Запрос2 (SELECT)
```

Тогда выведется таблица объединения записей из двух таблиц.
Единственное требование: чтобы количество полей совпадала и их типы тоже у нескольких запросов